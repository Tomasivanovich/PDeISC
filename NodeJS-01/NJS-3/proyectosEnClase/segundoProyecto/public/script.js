// Función la cual permite ver otras páginas al usarla
function verPagina(ruta) {    // Esta línea declara una función llamada verPagina que acepta un parámetro llamado ruta.
    
    // Esta línea le dice al navegador que cambie la página actual por la nueva URL que le pasamos.
    window.location.href = ruta;
}